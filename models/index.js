const Category = require('./category');
const ChatMessage = require('./messages-chat');
const Role = require('./role');
const Server = require('./server') ;
const User = require('./user') ;
const Product = require('./product');

module.exports = {
    Category,
    ChatMessage,
    Product,
    Role,
    Server,
    User,
}